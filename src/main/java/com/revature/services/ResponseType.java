package com.revature.services;

public enum ResponseType {
    SUCCESS,
    INVALID_USER,
    INVALID_EMAIL,
    ACCOUNT_EXIST,
    UNKNOWN_ERROR,
    USER_NOT_FOUND,
    INVALID_PASSWORD,
    INVALID_FIELDS,
    INVALID_CATEGORY,
}