package com.revature.models;

public class Administrator {

    /*public Administrator(String name, String email) {
        super(name, email);
    }*/

    // Adds Product to Catalog / Database
    public void addProduct(String name, String category, float price)
    {

        //Catalog.productList.add(new Product(name, category, price));

        // Todo : Add product to database
    }
}
